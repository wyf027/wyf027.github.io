# wu的小博客

Personal blog at https://wyf027.github.io/.

Restored from `wyf027/leno23.github.io` commit `d4cff01a5eadc4f9c7007d19d2aca7e0f50dd20d`, preserving its posts, theme, assets, and license. GitHub Pages publishes the `gh-pages` branch at `/` using Jekyll. Keep `.nojekyll` absent so Liquid templates and Sass are rendered.

The original installer remains available at `/install`.

Upstream theme documentation is in `README_CN.md`.

## GitHub résumé

`/resume/` self-hosts [resume/resume.github.com](https://github.com/resume/resume.github.com) at upstream commit `f5990756495bfd6d86495733bfb238c96771cbbf`, configured for `wyf027`. Data loads directly from GitHub public APIs; no token is stored. Recent contributions show the latest 100 merged public PRs.

The upstream theme is preserved, with Tailwind CSS 2.2.19 utilities for site navigation. jQuery 2.1.4, Mustache 0.3.0, YUI reset/fonts/grids 2.7.0, and Tailwind utilities are served locally. Upstream analytics are removed; the owner opted in explicitly.
